import assert from 'assert';
import mongoose from 'mongoose';

import config from '../config.json';

describe('Wifi', () => {
    it('should save wifi points', async () => {
        assert.ok(true);
    });
});